<?xml version="1.0" encoding="utf-8"?>
<!-- Plain solid fill matching colors.xml's uacast_background - ic_launcher_foreground (the
     provided UA CAST PLAYER artwork) is full-bleed/opaque, so this background never actually
     shows through; it only exists because every adaptive icon needs one. -->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">
    <path
        android:fillColor="#070A12"
        android:pathData="M0,0h108v108h-108z" />
</vector>
